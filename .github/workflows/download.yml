name: "Smart Download: Single MP3 or ZIP"
on:
  workflow_dispatch:
    inputs:
      yt_urls:
        description: 'הדבק קישורים (מופרדים בפסיקים)'
        required: true
      format:
        description: 'mp3 או mp4'
        default: 'mp3'
        required: true

jobs:
  youtube_job:
    runs-on: ubuntu-latest
    permissions: write-all
    
    steps:
      - name: 1. Checkout code
        uses: actions/checkout@v4

      - name: 2. Install Dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y ffmpeg
          python3 -m pip install --upgrade pip
          pip install yt-dlp

      - name: 3. Download Videos
        env:
          YT_COOKIES: ${{ secrets.YT_COOKIES }}
        run: |
          mkdir -p yt_output
          
          if [ -n "$YT_COOKIES" ]; then
            echo "$YT_COOKIES" > cookies.txt
            COOKIE_ARG="--cookies cookies.txt"
          else
            COOKIE_ARG=""
          fi

          FORMAT="${{ github.event.inputs.format }}"
          # פיצול הקישורים
          echo "${{ github.event.inputs.yt_urls }}" | tr ',' '\n' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | grep . > urls.txt

          while IFS= read -r CLEAN_URL || [ -n "$CLEAN_URL" ]; do
            echo "Processing: $CLEAN_URL"
            
            # הגדרות הורדה
            COMMON_ARGS="$COOKIE_ARG --ignore-errors --no-playlist --playlist-items 1 --no-restrict-filenames --windows-filenames --trim-filenames 100"
            OUTPUT_TEMPLATE="yt_output/%(title).100s.%(ext)s"

            if [[ ! "$CLEAN_URL" =~ ^http ]]; then
               SEARCH_PREFIX="ytsearch1:"
            else
               SEARCH_PREFIX=""
            fi

            if [ "$FORMAT" = "mp3" ]; then
              yt-dlp $COMMON_ARGS -x --audio-format mp3 --embed-thumbnail --add-metadata -o "$OUTPUT_TEMPLATE" "$SEARCH_PREFIX$CLEAN_URL"
            else
              yt-dlp $COMMON_ARGS -f "bv*[ext=mp4]+ba[ext=m4a]/b[ext=mp4]/best" --merge-output-format mp4 --embed-thumbnail --add-metadata -o "$OUTPUT_TEMPLATE" "$SEARCH_PREFIX$CLEAN_URL"
            fi
          done < urls.txt

      - name: 4. Process and Package (Python)
        id: process_files
        shell: python
        env:
          FILTER_CONTENT: ${{ secrets.FILTER_LIST }}
        run: |
          import os
          import shutil
          import re
          import zipfile

          filter_content = os.environ.get('FILTER_CONTENT', '')
          bad_words = [line.strip().lower() for line in filter_content.split('\n') if line.strip()]
          
          source_dir = "yt_output"
          if not os.path.exists(source_dir) or not os.listdir(source_dir):
              print("❌ No files downloaded!")
              exit(1)

          files = os.listdir(source_dir)
          valid_files = []

          for filename in files:
              # סינון מילים אסורות
              if any(word in filename.lower() for word in bad_words):
                  print(f"❌ Skipping filtered file: {filename}")
                  continue
              
              # ניקוי שם הקובץ מתווים בעייתיים למערכת הקבצים
              clean_name = re.sub(r'[\\/*?:"<>|]', '', filename)
              old_path = os.path.join(source_dir, filename)
              new_path = os.path.join(source_dir, clean_name)
              os.rename(old_path, new_path)
              valid_files.append(clean_name)

          if not valid_files:
              print("❌ No valid files remaining")
              exit(1)

          # יצירת הארטיפקט הסופי
          if len(valid_files) > 1:
              final_name = "collection.zip"
              with zipfile.ZipFile(final_name, 'w', zipfile.ZIP_DEFLATED) as zipf:
                  for f in valid_files:
                      zipf.write(os.path.join(source_dir, f), f)
          else:
              final_name = valid_files[0]
              shutil.move(os.path.join(source_dir, final_name), final_name)

          with open(os.environ['GITHUB_ENV'], 'a', encoding='utf-8') as f:
              f.write(f"FINAL_ARTIFACT={final_name}\n")

      - name: 5. Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "${{ env.FINAL_ARTIFACT }}"
          tag: "v${{ github.run_number }}-${{ github.run_id }}"
          name: "Download #${{ github.run_number }}"
          body: "ההורדה הושלמה בהצלחה!"
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
